<?xml version="1.0" encoding="utf-8"?>
<!--
    수온 관측소 선택 아이템 레이아웃
    Ocean Station Selection Item Layout

    iOS의 OceanSelectCell.swift에 대응
    RecyclerView의 각 행을 표시하는 레이아웃

    구성:
    - oceanName: 관측소 이름
    - checkBox: 선택 체크박스 (ImageView 사용)
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:background="?android:attr/selectableItemBackground"
    android:gravity="center_vertical">

    <!--
        관측소 이름
        Station Name
    -->
    <TextView
        android:id="@+id/tvOceanName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="관측소"
        android:textSize="16sp"
        android:textColor="@color/text_primary"
        android:gravity="start|center_vertical"
        android:paddingStart="20dp"
        android:paddingEnd="8dp" />

    <!--
        체크박스
        Checkbox

        ImageView를 사용하여 체크박스 구현
        선택 상태에 따라 checkbox_selected 또는 checkbox_normal 이미지 표시
    -->
    <ImageView
        android:id="@+id/ivCheckBox"
        android:layout_width="34dp"
        android:layout_height="34dp"
        android:src="@drawable/checkbox_normal"
        android:contentDescription="@string/content_description_checkbox"
        android:paddingEnd="4dp" />

</LinearLayout>
